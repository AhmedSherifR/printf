#include <limits.h>
#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
    int len = 1000;
    int len2 = 1000;

    _printf("Len:[%d]\n", len);
    printf("Len:[%d]\n", len2);

    return 0;
}
