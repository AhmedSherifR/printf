#ifndef _MAIN_H_
#define _MAIN_H_
#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>

int _printf(const char *format, ...);
int string_length(char *chr);
int print_string(char *ar);
int _putchar(int c);
int p_number(int ar);

#endif
